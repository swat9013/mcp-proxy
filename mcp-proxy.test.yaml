# テスト用設定（filesystem MCP サーバーを使用）

upstreams:
  filesystem:
    type: stdio
    command: "npx"
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "/tmp"
    allowedTools:
      - read_file
      - write_file
      - list_directory
